---
import { Image as AstroImage } from 'astro:assets'

interface Props {
  src: ImageMetadata
  alt?: string
}

const { src, alt } = Astro.props

const defaultAlt = alt ?? 'Default image'
---

<!-- This component -->
<AstroImage src={src} alt={defaultAlt} />
