<section class="marketing-grid" data-stagger>
  <article>
    <strong data-i18n="marketing1Title">Calendar-day math</strong>
    <p data-i18n="marketing1Body">Entry and exit dates are counted inclusively, matching the conservative rule most tax guidance uses.</p>
  </article>
  <article>
    <strong data-i18n="marketing2Title">Rolling windows</strong>
    <p data-i18n="marketing2Body">Switch between the current calendar year and a rolling 365-day view for stricter jurisdictions.</p>
  </article>
  <article>
    <strong data-i18n="marketing3Title">Guest-first privacy</strong>
    <p data-i18n="marketing3Body">Try the full workflow locally. Email sign-in exists only to save your trip log to your account.</p>
  </article>
  <article>
    <strong data-i18n="marketing4Title">Cautious by design</strong>
    <p data-i18n="marketing4Body">Use the count as a planning signal, then confirm your situation with a qualified advisor.</p>
  </article>
</section>

<style is:global>
/* ── Marketing grid ─────────────────────── */
  .deferred,
  .faq {
    margin-top: 24px;
    color: var(--muted);
  }

  .marketing-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 14px;
    margin-top: 28px;
  }

  .marketing-grid article {
    position: relative;
    overflow: hidden;
    border: 1px solid var(--line);
    border-radius: var(--radius-lg);
    padding: 20px;
    background: color-mix(in srgb, var(--surface) 88%, transparent);
    backdrop-filter: blur(8px);
    transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
  }

  .marketing-grid article:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow);
    border-color: color-mix(in srgb, var(--accent) 25%, var(--line));
  }

  .marketing-grid article::before {
    content: '';
    display: block;
    width: 38px;
    height: 38px;
    margin-bottom: 14px;
    border-radius: 10px;
  }

  .marketing-grid article:nth-child(1)::before {
    background: linear-gradient(135deg, var(--accent), var(--sky));
    box-shadow: 0 4px 12px color-mix(in srgb, var(--accent) 30%, transparent);
  }

  .marketing-grid article:nth-child(2)::before {
    background: linear-gradient(135deg, var(--sky), var(--accent-soft));
    box-shadow: 0 4px 12px color-mix(in srgb, var(--sky) 30%, transparent);
  }

  .marketing-grid article:nth-child(3)::before {
    background: linear-gradient(135deg, var(--success), var(--sky));
    box-shadow: 0 4px 12px color-mix(in srgb, var(--success) 30%, transparent);
  }

  .marketing-grid article:nth-child(4)::before {
    background: linear-gradient(135deg, var(--warn), var(--success));
    box-shadow: 0 4px 12px color-mix(in srgb, var(--warn) 24%, transparent);
  }

  .marketing-grid strong {
    color: var(--text);
    font-weight: 800;
    font-size: 1rem;
  }

  .marketing-grid p {
    margin: 8px 0 0;
    color: var(--muted);
    line-height: 1.6;
    font-size: 0.92rem;
  }
</style>
