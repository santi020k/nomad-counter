---
import CounterWorkspaceIsland from './CounterWorkspace.tsx'
import type { SSRInitialData } from '@/lib/app/ssrTypes'

interface Props {
  initialData?: SSRInitialData | null
}

const { initialData = null } = Astro.props
---

<CounterWorkspaceIsland client:load initialData={initialData} />
